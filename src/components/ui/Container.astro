---
interface Props {
    class?: string;
    as?: string;
    id?: string;
}

const { class: className = "", as: Tag = "div", id } = Astro.props;
---

<Tag id={id} class:list={["mx-auto max-w-7xl px-4 sm:px-6 lg:px-8", className]}>
    <slot />
</Tag>
